{
  "name": "selfie-verification-api",
  "version": "1.0.0",
  "private": true,
  "description": "Auth API for Selfie Verification (Express + SQLite locally / PostgreSQL in production)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start:api": "node ../scripts/run-api.js",
    "start:prod": "node src/db/migrate.js && node src/index.js",
    "dev": "node --watch src/index.js",
    "db:migrate": "node src/db/migrate.js",
    "db:seed-superadmin": "node src/db/migrate.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.3"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.10.0"
  }
}
